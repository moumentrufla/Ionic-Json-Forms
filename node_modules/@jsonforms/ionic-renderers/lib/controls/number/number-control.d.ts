import { NgRedux } from '@angular-redux/store';
import { ControlProps, JsonFormsState, RankedTester } from '@jsonforms/core';
import { JsonFormsControl } from '@jsonforms/angular';
import { LocaleService, LocaleValidation } from 'angular-l10n';
export declare class NumberControlRenderer extends JsonFormsControl {
    private localeService;
    private localeValidation;
    min: number;
    max: number;
    step: number;
    locale: string;
    displayValue: string;
    constructor(ngRedux: NgRedux<JsonFormsState>, localeService: LocaleService, localeValidation: LocaleValidation);
    getEventValue: (event: any) => number;
    mapAdditionalProps(props: ControlProps): void;
}
export declare const numberControlTester: RankedTester;
