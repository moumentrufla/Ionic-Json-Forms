import { ChangeDetectorRef } from '@angular/core';
import { Translation } from './translation';
import { LocaleService } from './locale.service';
/**
 * Provides 'lang' to the translate pipe,
 * 'defaultLocale', 'currency', 'timezone' to l10nDate, l10nDecimal, l10nPercent & l10nCurrency pipes.
 */
export declare class Localization extends Translation {
    protected changeDetectorRef: ChangeDetectorRef | undefined;
    defaultLocale: string;
    currency: string;
    timezone: string;
    protected locale: LocaleService;
    constructor(changeDetectorRef?: ChangeDetectorRef | undefined);
}
