import { LocaleConfig, TranslationConfig } from '../models/l10n-config';
import { LocaleService } from '../services/locale.service';
import { TranslationService } from '../services/translation.service';
/**
 * Initializes the services.
 */
export declare class L10nLoader {
    private localeConfig;
    private translationConfig;
    private locale;
    private translation;
    constructor(localeConfig: LocaleConfig, translationConfig: TranslationConfig, locale: LocaleService, translation: TranslationService);
    /**
     * Loads l10n services.
     */
    load(): Promise<void>;
}
