import { DefaultLocaleCodes } from './types';
export declare class DefaultLocale implements DefaultLocaleCodes {
    languageCode: string;
    scriptCode?: string;
    countryCode?: string;
    numberingSystem?: string;
    calendar?: string;
    value: string;
    private _value;
    build(languageCode: string, countryCode?: string, scriptCode?: string, numberingSystem?: string, calendar?: string): void;
    private parseValue();
}
