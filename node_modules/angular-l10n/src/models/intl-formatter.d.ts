import { NumberFormatStyle, DateTimeOptions } from './types';
export declare class IntlFormatter {
    static formatNumber(num: number, defaultLocale: string, style: NumberFormatStyle, digits?: string, currency?: string, currencyDisplay?: string): string | null;
    static formatDate(date: Date, defaultLocale: string, format: string | DateTimeOptions, timezone?: string): string;
    private static numberFormatter(num, defaultLocale, style, opts?);
    private static dateTimeFormatter(date, defaultLocale, format, timezone?);
}
